# core_backend_system standalone env example

# Server
CORE_HOST=0.0.0.0
CORE_PORT=????

# Tenant
DEFAULT_TENANT=yangchun

# AI flask server
AI_BASE_URL=http://127.0.0.1:????
AI_TIMEOUT_MS=120000

# DB connection is currently reused from ../DB/funcs/db_connection.js
# Ensure that module can connect using its own env vars (if any)
